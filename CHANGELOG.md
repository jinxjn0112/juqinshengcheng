# 更新记录

## 1.4.2

- 安卓平板不再按“平板布局”判断，统一按手机触控逻辑处理，横屏竖屏都使用移动端单列布局。
- 扩展入口改为 Android 更稳定的 pointerup / click 双兜底，不再依赖 touchend 才打开面板。
- 点击扩展入口时直接打开已经存在的剧情工坊面板，减少竖屏下点击被酒馆菜单层吃掉的情况。
- 移动端面板强制全屏，整页只保留一个主要纵向滚动区域，结果区、历史区和规则区不再抢滚动手势。
- 所有主要按钮、下拉框与输入框放大到触屏尺寸；手机和平板触控设备统一使用单列排列。
- 保留 1.4.1 的规则词、人物视角、楼层读取、独立 API、模型拉取等功能。

## 1.4.1

- 修复平板竖屏时扩展入口点击无反应的问题，增加触屏专用的点击兜底和更大的触控区域。
- 规则词编辑器改为在 v1.3.0 稳定增强层上以独立附加层加载，避免规则功能影响扩展本体打开。
- 竖屏下“AI 规则词 / 提示词”默认折叠，点击标题后展开，并自动滚动到合适位置，减少长面板导致的触控冲突。
- 保留 v1.4.0 的每选项独立规则词功能，生成时只注入当前实际生效的规则。
- 如果规则编辑器加载失败，剧情正文、人物视角、楼层读取、独立 API 与模型拉取仍可继续使用。

## 1.4.0

- 新增“AI 规则词 / 提示词”编辑区，可以为不同选项分别保存自己的写作规则。
- 规则分类包括：全局规则、生成类型、输出形式、人物视角、剧情方向、资料来源。
- “生成类型”可分别给剧情走向、新剧情、番外、大纲、伏笔工坊、灵感盲盒填写不同规则。
- “输出形式”可分别给剧情正文与灵感方案填写不同规则。
- “人物视角”可分别给自动判断、当前角色、用户角色、指定人物、全知视角填写不同规则。
- “剧情方向”可分别给自由推演、主线推进、支线开枝、抓住一点、人物驱动、世界设定驱动填写不同规则。
- “资料来源”可分别给聊天记录、人物设定、世界书 / 世界信息填写读取和使用规则。
- 生成时只会组合当前实际选中的规则，并在界面显示“本次会生效的自定义规则”。
- 自定义规则会作为明确写作要求送给模型，在不冲突于已知事实与安全规则的前提下优先遵守。
- 保留 v1.3.0 的独立 API、模型拉取、人物视角、楼层读取和横竖屏适配。

## 1.3.0

- 在 1.2.1 稳定版基础上重新接入增强层，去掉会反复触发的 MutationObserver 和按钮替换逻辑，改为一次性加载与捕获式事件接管，降低再次卡住酒馆启动页的风险。
- 保留“剧情正文 / 灵感方案”两种输出，升级后默认直接生成 1 份可阅读剧情正文。
- 保留人物视角：自动判断、当前角色、用户角色、指定人物、全知视角。
- 保留聊天楼层范围、人物设定、世界书、主线/支线/抓住一点等资料读取与剧情方向控制。
- 独立 API 现在支持三种模型选择方式：直接输入模型名、从已拉取列表中选择、点击“拉取模型”从 OpenAI 兼容的 `/models` 接口获取模型列表。
- 生成仍使用 OpenAI 兼容的 `/chat/completions`；若服务端限制浏览器跨域，模型名可以继续手动输入。
- 增强层加载失败时直接停止自身，不再持续观察或反复重建界面，基础插件仍可工作。

## 1.2.0

- 新增“输出形式”：默认生成可直接阅读的剧情正文，也可切回灵感方案。
- 文案数量默认改为 1，并保留手动调整数量。
- 新增人物视角选择：自动判断、当前角色、用户角色、指定人物、全知视角。
- 指定人物视角时可直接输入人物名字，生成时会尽量限制在该人物的感知与认知范围内。
- 剧情正文模式会直接输出完整场景，不先列方案或分析；大纲与伏笔模式仍保留原本结构，并补充可读示例。
- 顶部说明改为同时强调“找灵感”和“直接生成剧情正文”。

## 1.1.1

- 修复平板横屏时面板高度和内部滚动区域计算不稳定的问题。
- 横屏改为工作区与灵感记录分别纵向滚动，短屏时自动压缩标题区与卡片高度。
- 竖屏改为单一纵向滚动容器，避免多层滚动互相抢手势。
- 适配动态视口高度与安全区，减少浏览器地址栏、底部导航栏导致的内容被截断。
- 增加更明显的滚动条与触摸惯性滚动支持。

## 1.1.0

- 为剧情灵感工坊增加独立 API 配置，并保留“使用酒馆当前模型”作为备用方式。
- 增加聊天楼层读取范围：最近剧情，或指定楼层并分别设置向上/向下读取数量。
- 增加可选资料源：聊天记录、当前人物设定、世界书；每次生成前会显示将读取的范围。
- 增加剧情聚焦方式：自由推演、主线推进、支线开枝、抓住一点、人物驱动、世界设定驱动。
- “抓住一点”可以填写具体信息，并指定更偏主线、支线或两者都要。
- 世界书支持“相关条目优先 / 全部启用条目”，并允许手动补充世界书名称。
- 横屏平板改为三栏工作台，竖屏自动切换为纵向布局；手机继续保持单列。
- 保留生成、细化、复制、填入输入框、换一批和当前聊天灵感记录。

## 1.0.0

- 初版剧情走向、新剧情、番外、大纲、伏笔与灵感盲盒。
